# Master Chocolate — merged PC + mobile build

PC uses the original 96-frame scroll animation from the supplied Master Chocolate project.
Mobile uses the separate 3D rotating packet hero from the recent mobile build.

Open index.html with VS Code Live Server.

Desktop: 96-frame scroll reveal.
Mobile: independent CSS 3D rotating packet hero; desktop canvas is hidden and frames are not preloaded.
